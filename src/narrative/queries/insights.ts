import type { KnowledgeArtifact, ImpactArtifact } from "@/components/narrative/types";

/**
 * Placeholder query layer for future KnowledgeArtifacts (Phase 7E).
 * Currently returns empty array, preserving schema compatibility.
 */
export function getKnowledgeArtifacts(): KnowledgeArtifact[] {
  return [];
}

/**
 * Placeholder query layer for future ImpactArtifacts (Phase 7E).
 * Currently returns empty array, preserving schema compatibility.
 */
export function getImpactArtifacts(): ImpactArtifact[] {
  return [];
}
