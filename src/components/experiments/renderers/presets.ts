import * as THREE from "three";
import type { GlassPreset, GlowPreset } from "@/types";

export const GLASS_PRESETS: Record<GlassPreset, THREE.MeshPhysicalMaterialParameters> = {
  emerald: {
    color: "#10b981",
    transmission: 0.9,
    opacity: 1,
    metalness: 0.1,
    roughness: 0.15,
    ior: 1.5,
    thickness: 0.5,
    specularIntensity: 1,
    specularColor: new THREE.Color("#ffffff"),
    clearcoat: 1,
    clearcoatRoughness: 0.1,
  },
  amber: {
    color: "#f59e0b",
    transmission: 0.85,
    opacity: 1,
    metalness: 0.2,
    roughness: 0.1,
    ior: 1.52,
    thickness: 0.6,
    specularIntensity: 1,
    specularColor: new THREE.Color("#ffffff"),
    clearcoat: 1,
    clearcoatRoughness: 0.05,
  },
  copper: {
    color: "#d97706",
    transmission: 0.8,
    opacity: 1,
    metalness: 0.4,
    roughness: 0.2,
    ior: 1.6,
    thickness: 0.4,
    specularIntensity: 1,
    specularColor: new THREE.Color("#ffffff"),
    clearcoat: 0.8,
    clearcoatRoughness: 0.1,
  },
  slate: {
    color: "#64748b",
    transmission: 0.7,
    opacity: 1,
    metalness: 0.5,
    roughness: 0.3,
    ior: 1.45,
    thickness: 0.5,
    specularIntensity: 0.8,
    specularColor: new THREE.Color("#e2e8f0"),
    clearcoat: 0.5,
    clearcoatRoughness: 0.2,
  },
  clear: {
    color: "#ffffff",
    transmission: 0.95,
    opacity: 1,
    metalness: 0.05,
    roughness: 0.05,
    ior: 1.5,
    thickness: 0.3,
    specularIntensity: 1,
    specularColor: new THREE.Color("#ffffff"),
    clearcoat: 1,
    clearcoatRoughness: 0.05,
  },
};

export const GLOW_PRESETS: Record<GlowPreset, string> = {
  green: "#10b981",
  gold: "#f59e0b",
  blue: "#3b82f6",
  crimson: "#ef4444",
  none: "#000000",
};
